{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "holizuki.serviceAccountName" . }}
  labels:
    {{- include "holizuki.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
